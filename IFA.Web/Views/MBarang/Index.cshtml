
@{
    ViewData["Title"] = "Master Barang";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script>
    var urlGetData = '@Url.Action("GetAll", "MBarang")';
    var urlForm = '@Url.Action("EditBrg", "MBarang")';
    var urlSave = '@Url.Action("SavelistBarang", "MBarang")';
    var urlDelete = '@Url.Action("DeleteBrg", "MBarang")';
    var urlUpdate = '@Url.Action("UpdateBrg", "MBarang")';
    var urlSaveUpload = '@Url.Action("saveUpload", "MBarang")';
    var BarangCbo = @(Html.Raw(ViewBag.BarangList));
    var PersediaanCbo = @(Html.Raw(ViewBag.PersediaanList));
    var Penjualan2Cbo = @(Html.Raw(ViewBag.Penjualan2List));
     var SatuanCbo = @(Html.Raw(ViewBag.Satuan));
</script>

<script type="text/javascript" src="~/Script/Master/MBarang.js" asp-append-version="true"></script>

<div id="wrapperList">
    <div class="panel-title">
        <span class="fontTitle">Master Barang</span>

    </div>
    <div class="col-md-12" style="margin-bottom: 15px;">
        <div class="col-md-8">
            <div class="col-md-4">
                <div class="">

                  
                </div>
            </div>

        </div>
        <div class="col-md-4">
            <button id="btnPrint" class="btn btn-info pull-right" onclick="printpage();" style="margin-right: 15px;display:none;"><i class="glyphicon glyphicon-print"></i>&nbsp; Cetak</button>

        </div>
    </div>

    @*<div>
        <button id="addBrg" class="btn btn-primary pull-right" onclick="showForm()"><i class="glyphicon glyphicon-plus"></i>&nbsp; Tambah Barang</button>
    </div>*@
   
    <div id="GridBrg" style="display:inline-block;"></div>
</div>
<div id="editForm"></div>
<div id="dialog"></div>



